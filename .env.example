# Required for the /api/match semantic-scoring endpoint.
# Get a key at https://console.anthropic.com/settings/keys
# Locally: `vercel dev` reads this from .env (copy this file to .env and fill it in).
# On Vercel: set it in Project Settings -> Environment Variables instead.
ANTHROPIC_API_KEY=sk-ant-...
